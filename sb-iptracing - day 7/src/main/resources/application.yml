server:
  port: 8046

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create-drop
  application:
    name: sb-iptracing
app:
  ipUrl: 'http://ipinfo.io/'
  ipToken: df60febe15ccd2

